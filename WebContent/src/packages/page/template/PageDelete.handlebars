{{#if currentPage.children}}
	<p>You can not delete a parent page containing children!</p>
{{else}}
	<p>
	Are you sure you want to delete page "{{currentPage.menuTitle}}"?
	</p>
	
	<button {{ action "confirm" }}>
	Confirm
	</button>
{{/if}}

<a {{ action "gotoShow" currentPage href=true }}>
Back
</a>