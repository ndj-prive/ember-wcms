{{#if isLoggedIn}}

<div id="pageContentEditor" contenteditable="true">
    {{{unbound currentPage.content}}}
</div>

<button id="pageSave" class="btn" {{ action "save" target="view" }}>
Save
</button>

{{else}}

<div id="pageContentEditor">
    {{{currentPage.content}}}
</div>

{{/if}}
