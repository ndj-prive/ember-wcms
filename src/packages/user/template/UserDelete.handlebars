<p>
    Are you sure you want to delete user "{{currentUser.firstName}} {{currentUser.lastName}}"?
</p>

<p>
    <button class="btn btn-primary" {{action "confirm"}}>
    Confirm
    </button>
    {{#linkTo user.show currentUser class="btn"}}
    Back
    {{/linkTo}}
</p>
